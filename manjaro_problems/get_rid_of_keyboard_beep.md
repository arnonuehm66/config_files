# Turn beep off

Once per session type into terminal:

```bash
>$ xset -b
```

To do it at startup add it to an autostart. In Manjaro xfce4's setting can be utilzed.

Go to `Settings`, click `Session and Startup` and add a new entry with `xset -b` at `log in`.
